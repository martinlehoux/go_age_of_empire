# https://taskfile.dev

version: '3'

vars:
  GREETING: Hello, World!

tasks:
  build:
    cmds:
      - go build -o bin/go_age_of_empire *.go

  run:
    deps: [build]
    cmds:
      - ./bin/go_age_of_empire
